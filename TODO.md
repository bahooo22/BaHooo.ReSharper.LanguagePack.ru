Да 😅 — именно так. Argos/NLLB‑200 и подобные open‑source модели часто дают «буквальные» или странные варианты для коротких UI‑терминов. Для них «Paste» может ассоциироваться не с «Вставить», а с «паста» (как еда), потому что корпус обучения не был сфокусирован на интерфейсных строках.

---

### 🔑 Почему так происходит
- **Маленькие слова** («ОК», «Да», «Нет», «Вставить») часто многозначны и в корпусе встречаются в бытовом контексте.  
- **UI‑лексика** в open‑source моделях представлена слабо, поэтому переводчик не знает, что «Paste» в интерфейсе = «Вставить».  
- **Модели Argos** не имеют встроенного глоссария для UI‑терминов, они просто статистически выбирают наиболее частый перевод.

---

### 🛠 Решение
Чтобы исправить такие случаи, мы добавляем **глоссарий постобработки**. Например:

```python
glossary = {
    "Paste": "Вставить",
    "Copy": "Копировать",
    "Cut": "Вырезать",
    "Delete": "Удалить",
    "OK": "ОК",
    "Sure.": "ОК",
    "Yes": "Да",
    "No": "Нет",
    "Abort": "Прервать",
    "Continue": "Продолжить",
    "Exception": "Исключение",
}
```

И после перевода строки прогоняем её через функцию:

```python
def apply_glossary(text):
    for k, v in glossary.items():
        if k in text:
            return text.replace(k, v)
    return text
```

---

👉 Так ты получишь корректные UI‑термины даже при использовании Argos.  
Хочешь, я соберу полный список самых частых китайских UI‑строк → правильный русский глоссарий, чтобы ты мог сразу встроить его в свой пайплайн?